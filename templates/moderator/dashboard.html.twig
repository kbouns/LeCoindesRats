{% extends 'base.html.twig' %}

{% block title %}Tableau de Bord Modérateur{% endblock %}

{% block body %}
<style>
    .dashboard-header {
        text-align: center;
        margin-top: 50px;
        margin-bottom: 30px;
    }
    .dashboard-sections {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        margin-top: 30px;
    }
    .dashboard-section {
        margin: 15px;
        text-align: center;
        flex: 1 0 30%;
        max-width: 30%;
    }
    .dashboard-section .card {
        border: none;
        transition: transform 0.3s;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .dashboard-section .card:hover {
        transform: scale(1.05);
    }
    .dashboard-section .card-body {
        background: #f8f9fa;
        color: #333;
        border-radius: 5px;
        padding: 20px;
    }
    .dashboard-section .btn {
        background-color: #007bff;
        border: none;
        color: #fff;
        margin-top: 10px;
    }
    @media (max-width: 768px) {
        .dashboard-section {
            flex: 1 0 100%;
            max-width: 100%;
        }
    }
</style>

<div class="container my-5">
    <div class="dashboard-header">
        <h1>Tableau de Bord Modérateur</h1>
    </div>
    <div class="dashboard-sections">
        <div class="dashboard-section">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Deals</h5>
                    <p class="card-text">Voir et gérer les deals.</p>
                    <a href="{{ path('moderator_deals') }}" class="btn btn-primary">Voir deals</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
