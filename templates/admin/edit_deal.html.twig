{% extends 'base.html.twig' %}

{% block title %}Modifier Deal{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier Deal</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('admin_deals') }}" class="btn btn-secondary mt-3">Retour</a>
</div>
{% endblock %}
